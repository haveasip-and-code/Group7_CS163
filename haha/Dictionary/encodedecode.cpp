#pragma once
#include <bits/stdc++.h>
#include "encodedecode.h"
#include "generalLib.h"

string encode(u32string s) {
    int _n=s.length();
    for (int ii=0;ii<_n;ii++) {
        cout<<s[ii]<<'\n';
    }
}
