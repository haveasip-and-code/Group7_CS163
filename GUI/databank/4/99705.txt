ibook
-A crappy computer from Macintosh(crapy anyways) that uses a small battery that can't run a small machine. ; -A computer that can't run its own [apple works] ; -Machine that locks up ONLY 4 times a day

